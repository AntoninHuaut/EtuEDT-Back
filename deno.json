{
    "tasks": {
        "start": "ENV=prod deno run --check=all --import-map=import_map.json --unstable --allow-net --allow-env --allow-read=./ ./src/app.ts",
        "dev": "ENV=dev deno run --check=all --import-map=import_map.json --unstable --allow-net --allow-env --allow-read=./ --watch ./src/app.ts",
        "update": "deno run -A https://deno.land/x/udd/main.ts import_map.json"
    }
}